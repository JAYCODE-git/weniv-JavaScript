

// 고급 템플릿 리터럴(React)
// Template literal은 function!

const x = 10;
const y = 20;
const result = x * y
// 템플릿리터럴 중괄호 안에 연산을 넣는 것은 권장하지 않음.
console.log(`x 값은${ x }, y값은 ${ y } 둘을 곱한 값은 ${ x * y }`);
console.log(`x 값은${ x }, y값은 ${ y } 둘을 곱한 값은 ${ result }`);
console.log(`
 h
 e
 l
 l
 o`);
